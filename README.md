1) What is the difference between null and undefined?  
 - Null respresents an intentional absence of value, explicity assainged by a devleloper , while nudefined signifies an uninitialized sate where a variable has been declared but not yet assigned a value.
3) What is the use of the map() function in JavaScript? How is it different from forEach()?  
 - map() is used to transform each element in an array and return a new array containing the result, with out modifying the orginal array.
 - forEach() executes a function for side effects and always returns undefined. 
5) What is the difference between == and ===?
- '==' is compare only value and '===' is compare value and type'
6) What is the significance of async/await in fetching API data?
- The significance of async/await in fetching API data lies in its ability to let developers write asynchronous code that looks and behaves like synchronous code, which greatly improves readability, maintainability, and error handling compared to traditional callbacks or promise chains. 
7) Explain the concept of Scope in JavaScript (Global, Function, Block).
- Global Scope: Variables declared outside any function or block are accessible anywhere in the program.  
- Function Scope: Variables declared inside a function are only accessible within that function.  
- Block Scope: Variables declared with let or const inside {} are only accessible within that specific block.
